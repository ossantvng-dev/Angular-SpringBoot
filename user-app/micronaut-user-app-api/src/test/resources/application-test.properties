jpa.default.properties.hibernate.hbm2ddl.auto=none

# The datasource URL/credentials live in application-dev/prod, which are not active
# under the 'test' environment - repeated here so config still resolves.
# Testing itself is out of scope for this migration (decision 8 in design-plan.md).
datasources.default.url=jdbc:mysql://localhost:3306/usersdb?allowPublicKeyRetrieval=true&useSSL=false
datasources.default.username=usersdb_user
datasources.default.password=password

redis.uri=redis://localhost:6379
